import React from "react";
import styled from "styled-components";

const Crop_prediction = () => {
  return <div>Crop_prediction</div>;
};

export default Crop_prediction;

const Container = styled.div`
  height: calc(100vh - 9.3rem);
`;
